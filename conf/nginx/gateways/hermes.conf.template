# Hermes 端口网关：0.0.0.0:6060 → 127.0.0.1:6060（直通，零路径改写）
server {
    listen ${GATEWAY_IP}:6060;

    auth_basic "Hermes Direct";
    auth_basic_user_file /etc/nginx/.htpasswd;
    include /etc/nginx/snippets/allowed-ips.conf;

    location / {
        proxy_pass http://127.0.0.1:6060;
        include /etc/nginx/snippets/proxy-headers.conf;
        include /etc/nginx/snippets/ws.conf;
        proxy_buffering off;
        proxy_read_timeout ${PROXY_READ_TIMEOUT}s;
    }
}
