# WebSocket 升级（Connection 头由 http 块 map 动态生成）
# 注意: 本片段不含 proxy_read_timeout —— 由各 location 按场景自行设置，
#       避免与本块内的 timeout 指令重复导致 nginx 配置非法。
proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $connection_upgrade;
