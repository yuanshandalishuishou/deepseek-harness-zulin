# DeepSeek Harness 租赁版（deepseek-harness-zulin）— 技术方案

> **文档定位**：这是「实现视角的方案」，描述**怎么落地**用户需求.md 中的要求（对应文件 `用户需求.md`）。
> **文档状态**：代码改动已落地（本地提交，**未推送**，未触发 GHCR 构建），待用户确认需求后进入代码复查 + README 重生成。
> **约定**：下列行号指撰写时的代码位置，复查时以实际为准。

---

## 1. 总体架构

```
                GitHub Actions (境外 runner)
   push main ──► docker build ──► ghcr.io/.../deepseek-harness-zulin:latest
                                          │  (镜像已含官方 TFG 二进制 + socat + bun + chromium)
                                          ▼
   用户/NAS:  docker pull  ──►  docker run  ──►  容器内服务编排 (entrypoint.sh)
                                          │
   ┌──────────────────────────────────────┴──────────────────────────────────────┐
   │  SSH(22)  xRDP(3389)  Harness Web(3080)  OpenClaw(18789)  Hermes(3000/8080)  │
   │  TFG 二进制 ── 监听 127.0.0.1:34560 ──► socat 0.0.0.0:3456 ──► 宿主 13456   │
   │  管理端口(16688): mgmt.py（随机凭据/改配置/热重启/状态页/审计）              │
   └─────────────────────────────────────────────────────────────────────────────┘
   持久化卷: dsh-data(/root/.dsh)  dsh-tfg-auth(/root/.token-free-gateway)  dsh-chrome-tfg(/root/.chrome-tfg-debug)
```

核心设计点：
- **镜像零密钥**：所有 Key / 令牌 / 密码仅在容器首次启动由 `entrypoint.sh` 按 `-e` 环境变量注入。
- **TFG 来源唯一可信**：仅构建期从官方 Releases 下载并 sha256 校验（防投毒）。
- **13456 根治**：TFG 监听内部端口，socat 自动转发对外端口（F6），与官方二进制默认绑定地址解耦。

---

## 2. 构建流水线（Dockerfile）

### 2.1 TFG 官方下载（A5 / H9 / Q2）
- 触发：每次 `docker build`（GHCR 境外 runner 或本地回退构建）。
- 实现：Dockerfile 中以 `RUN` 在**构建阶段**直接 `curl` 官方 Releases：
  - `TFG_VERSION=latest` → `releases/latest/download`；否则 `releases/download/<ver>`。
  - 下载 `checksums-sha256.txt` 与 `token-free-gateway-linux-x64.tar.gz`（`--retry 5 --retry-delay 3 --retry-all-errors`）。
  - 用官方校验和比对实际 sha256，**不一致立即 `exit 1`** 中止构建。
  - 解包至 `/usr/local/bin/token-free-gateway`。
  - 顺带安装 `bun` 与 `chromium`，`git clone` 官方源码到 `/opt/token-free-gateway`（仅兜底 `bun` 运行，clone 失败不中止）。
- **关键约束**（Q2）：**不提供任何跳过/切换下载的开关**，GHCR 与本地构建恒为官方下载；**绝不**采用「宿主机本地下载后 COPY 进镜像」。

### 2.2 socat 安装（支撑 F6）
- Dockerfile 已把 `socat` 并入既有 `apt-get install` 列表（与 `chromium` 同一条 `RUN`），用于运行时端口转发。

### 2.3 多源加速（A4）
- 国内本地回退构建可用 `--build-arg DEBIAN_MIRROR / NODE_DIST / NPM_REGISTRY` 加速，但 **TFG 下载始终走官方 GitHub**（不受镜像源影响）。

---

## 3. 运行时启动逻辑（entrypoint.sh）

### 3.1 凭据与审计（C/G/E5）
- `ROOT_USER` 默认 `root`；`ROOT_PASSWORD` 默认 `deepseek`（**Q3：保留固定默认，仅 `-e` 覆盖**）。
- 检测 `ROOT_PASSWORD` 是否由用户显式传入；若是，调用 `audit("set_ssh_password", ...)` 记入审计日志。
- 新增顶层 `audit()` 函数，写 `/var/log/dsh-audit.log`，格式：`[时间戳] action=动作 user=用户 detail=详情`。

### 3.2 TFG 默认开启 + 自动转发（F3 / Q1 / F6）
改动要点（约第 313 行起）：
```bash
ENABLE_TOKEN_FREE_GATEWAY="${ENABLE_TOKEN_FREE_GATEWAY:-1}"   # Q1：默认开启
# 归一化：1/true/yes/on/enabled → 启用；0/false/空 → 关闭
TFG_PORT="${TFG_PORT:-3456}"                  # 对外暴露端口（宿主 13456:3456）
TFG_INNER_PORT="${TFG_INNER_PORT:-34560}"     # TFG 实际监听端口（内部）
```
启动逻辑：
1. `audit("enable_tfg", ...)` 记录启用。
2. 按 `TFG_LOGIN_MODE`（默认 `xrdp`）：xrdp 模式不预拉浏览器；headless 模式拉无头 Chromium(CDP 9222)。
3. 启动网关二进制，但**仅让它监听内部端口**：
   ```bash
   nohup env TFG_PORT="$TFG_INNER_PORT" TFG_CDP_URL="$TFG_CDP_URL" TFG_API_KEY="$TFG_API_KEY_VAL" \
       $GW serve > /var/log/token-free-gateway.log 2>&1 &
   ```
   > 说明：`$GW` 优先 `/usr/local/bin/token-free-gateway`，缺失则回退 `bun /opt/token-free-gateway/index.ts`。
4. 轮询等待内部端口 `/health` 就绪。
5. **socat 自动转发（F6 核心）**：
   ```bash
   socat TCP-LISTEN:${TFG_PORT},bind=0.0.0.0,fork,reuseaddr TCP:127.0.0.1:${TFG_INNER_PORT}
   ```
   这样无论官方二进制默认绑 127.0.0.1 还是 0.0.0.0，宿主 `13456` 都能经 `3456` 抵达 TFG（根治历史「13456 不通」）。
6. 对外端点固定为 `http://localhost:3456/v1`（OpenAI 兼容），与内部端口解耦。

### 3.3 端口导出给管理端口（支撑 E4）
启动管理端口前执行：
```bash
export MGMT_PORT WEB_PORT OPENCLAW_PORT HERMES_WEB_PORT HERMES_DASH_PORT TFG_PORT
```
使 `mgmt.py` 能读取各服务端口用于状态探测。

---

## 4. 管理端口增强（mgmt/mgmt.py）

### 4.1 聚合状态页（E4）
- 新增 `aggregate_status()`：对 8 项组件做 TCP/HTTP 探测：
  - Harness Web（`/`，HTTP）、OpenClaw/ Hermes Web / Hermes 面板 / SSH / xRDP / Chromium CDP（TCP）、TFG（`/health`，走 3456→socat→内部）。
- 新增路由：
  - `GET /status`：**免登录**只读 HTML 概览（仅显示组件名/状态/探测端点，不含任何敏感信息）。
  - `GET /api/status`：需登录，返回 JSON `{components:[{name,ok,endpoint}...]}`。
- 新增 `STATUS_PAGE` 模板与 `_esc`/`_probe_tcp`/`_probe_http` 辅助函数。

### 4.2 操作审计（E5）
- 新增 `audit(action, detail)` 写 `/var/log/dsh-audit.log`，并通过模块级 `_CURRENT_USER` 记录当前登录用户。
- 在以下处理分支追加审计调用：
  - `login`（成功）、`change_password`、`config_write`/`config_write_failed`、
  - `tfg_apply`、`tfg_webauth`、`gen_openclaw_token`、`apply_role`、`restart`/`restart_failed`。

---

## 5. 安全设计

- **零密钥镜像（G1）**：TFG / OpenClaw / Hermes / Harness 的 Key 全部运行时注入，镜像内无硬编码密钥。
- **TFG 不鉴权仅内网（G4）**：`TFG_API_KEY` 留空时不鉴权，仅建议内网；设值后全接口鉴权（`/health` 免鉴权）。
- **审计溯源（E5）**：所有特权操作留痕，便于多管理员场景定责。
- **密码覆盖留痕（G2/Q3）**：`-e ROOT_PASSWORD` 覆盖默认 `deepseek` 时记入审计。

---

## 6. 公网部署检查清单（G3）

> 当容器需暴露到公网时，**必须**逐项核对：

1. **防火墙 / 安全组**：仅放行必要端口；管理端口 `16688`、SSH `10022`、xRDP `13389` 不应直接公网开放，建议仅对可信 IP 开放或走 VPN/隧道。
2. **TFG 鉴权（F8）**：公网暴露 TFG（`13456`）**必须**设 `TFG_API_KEY`，否则免 Key 网关会被任意调用（可在后续版增加「检测公网 IP 且未设 Key 即拒绝/告警」的硬护栏）。
3. **管理端口强密码**：首次登录 `16688` 后**立即修改**随机凭据；不要长期使用初始随机密码。
4. **SSH/xRDP 密码**：公网暴露时务必 `-e ROOT_PASSWORD=<强密码>` 覆盖默认 `deepseek`（覆盖已计入审计）。
5. **反向代理 + TLS**：Web/管理类 HTTP 服务建议前置 Nginx/Caddy 做 TLS 终止与鉴权，避免明文传输凭据。
6. **数据卷备份**：定期备份 `dsh-data` / `dsh-tfg-auth` / `dsh-chrome-tfg`（H4 后续提供脚本）。
7. **镜像更新**：关注 GHCR `:latest` 更新，必要时用 watchtower 或手动 `docker pull` + 重建。
8. **审计日志外送**：将 `/var/log/dsh-audit.log` 纳入集中日志，便于安全审计。

---

## 7. 本次文件改动清单

| 文件 | 改动点 | 对应需求 |
|------|--------|----------|
| `Dockerfile` | TFG 下载注释「默认开启」；`apt-get` 增加 `socat`（支撑 F6） | A5/F6/Q1 |
| `entrypoint.sh` | 新增顶层 `audit()`；`ENABLE_TOKEN_FREE_GATEWAY` 默认 `1`；密码覆盖写审计；TFG 块改为「内部端口 + socat 自动转发」；启动 mgmt 前 `export` 端口 | F3/Q1/F6/E5 |
| `mgmt/mgmt.py` | 导入 `socket`；读取各服务端口；`_CURRENT_USER`；`audit()`；`aggregate_status()` + `/status`、`/api/status`；各特权操作补审计调用 | E4/E5 |
| `deploy.sh` | 已默认 `ENABLE_TOKEN_FREE_GATEWAY=1`（与 Q1 一致，无需改） | Q1/H3 |
| `需求规格说明书-待确认版.md` | 拆分为 `用户需求.md` 与 `技术方案.md`（本文件），原待确认版移除 | — |

---

## 8. 验证计划（构建/部署后）

1. **构建**：观察 GHCR Actions 日志含 `[TFG] 校验通过`；`docker pull` 后 `docker run --rm <image> /usr/local/bin/token-free-gateway --version` 正常。
2. **TFG 默认开**：不加开关直接 `docker run -p 13456:3456 ...`，`curl http://宿主IP:13456/health` 应返回 JSON；`curl http://宿主IP:13456/v1/models` 可列出模型。
3. **开关归一化**：分别用 `-e ENABLE_TOKEN_FREE_GATEWAY=true|yes|on|enabled` 验证启用；`-e ...=0|false` 验证关闭（13456 不通）。
4. **13456 根治**：容器内 `ss -tlnp` 确认 `socat` 监听 `0.0.0.0:3456`、`token-free-gateway` 监听 `127.0.0.1:34560`；宿主 13456 可达。
5. **状态页**：浏览器开 `http://<mgmt-host>:16688/status` 看 8 项状态；登录后 `/api/status` 返回 JSON。
6. **审计**：执行改密/改配置/重启后 `cat /var/log/dsh-audit.log` 可见对应记录。
7. **密码审计**：带 `-e ROOT_PASSWORD=xxx` 启动后，审计日志含 `set_ssh_password`。

---

## 9. 风险与假设

- **假设**：官方 TFG 二进制**尊重 `TFG_PORT` 环境变量**决定监听端口。若其忽略该变量而固定监听某端口，则 socat 转发端口需相应调整（验证计划第 4 步可确认）；必要时改为传命令行参数或探测实际监听端口。
- **镜像未推送**：本次改动仅本地提交，**未 push、未触发 GHCR 构建**。确认需求后需 `git push` 触发 Actions 重建镜像，再走验证计划。
- **F8 / E3 / B4 / H4 / A8** 等列为后续增强，本次未实现，不阻塞当前验收。

---

*本文件为「技术方案」，对应需求见 `用户需求.md`。确认后进入代码复查与 `README.md` 重生成。*
