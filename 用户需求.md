# DeepSeek Harness 租赁版（deepseek-harness-zulin）— 用户需求

> **文档定位**：这是「用户视角的需求」，描述**要做什么、为什么、达到什么标准**。对应的「怎么实现」见同级文件 **技术方案.md**。
> **文档状态**：基于你在本轮确认的决策（Q1–Q6）整理，代码改动已落地（未推送），待你审核后进入「代码复查 + 重生成 README」阶段。
> **标注约定**：【已有】历史已提出且已实现／【优化】对既有要求的改进／【新增】本次建议新增（你已拍板是否采纳）。

---

## 0. 产品目标（一句话）

把 [deepseek-ai/deepseek-harness](https://github.com/deepseek-ai/deepseek-harness) 与为**央企融资租赁**场景定制的「八位专家」多智能体角色、三大网关（DeepSeek Harness Web / OpenClaw / Hermes）、在线**管理端口 16688**、以及**免 Token 的 Token-Free Gateway** 打包为一个开箱即用的 Debian 13 容器；通过 GitHub Actions「推送即构建」发布到 GHCR；镜像**零密钥**，所有敏感配置在首次启动由环境变量注入。

---

## 1. 你的决策结论（Q1–Q6）

| 编号 | 议题 | 你的决定 | 对需求的影响 |
|------|------|----------|--------------|
| **Q1** | TFG 默认开 / 关 | **默认开**（置 `0/false/空` 才关） | 推翻此前「默认关」建议；`deploy.sh`、entrypoint、README 三方默认统一为**开** |
| **Q2** | 是否加「本地构建可跳过 TFG 下载」开关 | **不加**，恒为官方下载 | 原建议 A7 移除；GHCR 与本地构建**都**从官方库下载（构建期 `RUN`，非宿主下载后 COPY） |
| **Q3** | SSH/xRDP 默认密码 | **保留 `deepseek`**，仅当运行容器用 `-e` 指定时覆盖 | 不采纳随机强密码（C4/G2 暂不实施）；但 `-e` 覆盖密码会写入审计日志 |
| **Q4** | 是否纳入以下增强 | **全部纳入**：F6（socat 自动转发）/ E4（聚合状态页）/ E5（审计日志）/ G3（公网部署清单） | 四项均进入本次实现 |
| **Q5** | ARM64 支持 | **暂不启用**（仅 linux/amd64） | A8 延后 |
| **Q6** | TFG 版本策略 | **保持 `latest`**（每次取官方最新 linux-x64） | 不锁定 `v0.5.1` |

---

## 2. 历史要求归并（源自多轮对话）

| 序号 | 历史要求（原始意图） | 当前落点 |
|------|----------------------|----------|
| H1 | 基于 deepseek-harness 构建容器，内置「八位专家」央企融资租赁多智能体角色 | souls/ 8 个角色 + 默认 persona enterprise-boss |
| H2 | 推 GitHub + CI/CD「推送即构建」发布 GHCR | `.github/workflows/docker-image.yml`（main / v* / 手动） |
| H3 | Web UI 端口原错配，修正为 13000→3080 | deploy.sh / README 已对齐 |
| H4 | 多模型可切换：DeepSeek 官方 / 硅基流动 / 阿里百炼 / 自定义 DS / 自定义 OpenAI | `MODEL_CHOICE` 1–5 |
| H5 | 完整桌面 + 远程接入（Xfce4 + xRDP + OpenSSH） | Dockerfile + entrypoint |
| H6 | 镜像**不含任何 API Key**，运行时注入生成 `settings.yaml` | entrypoint 首次启动写 `/root/.dsh/settings.yaml` |
| H7 | 三大网关：Harness Web(3080) / OpenClaw(18789) / Hermes(3000+8080) | 已集成 |
| H8 | 管理端口 16688：在线改令牌/模型/Key + 热重启；首启随机凭据强改密 | `mgmt/mgmt.py` |
| **H9** | **TFG 必须每次构建从官方库 andeya/token-free-gateway 下载最新 linux-x64，禁用镜像站/fork/第三方转存（防投毒）** | Dockerfile `RUN` 官方下载 + sha256 校验 |
| **H10** | TFG 经浏览器会话免 Key 调 13+ 家模型，OpenAI 兼容 /v1；用开关启用 | `ENABLE_TOKEN_FREE_GATEWAY`（接受 1/true/yes/on/enabled） |
| H11 | 放通 `--host 0.0.0.0` | `patch-web-bind.sh` |
| H12 | 修复浏览器 `crypto.randomUUID is not a function` | 服务端 + 浏览器端 polyfill |
| H13 | 数据持久化（角色/配置/对话） | 卷 `dsh-data` + TFG 卷 |
| H14 | deploy.sh 一键部署（GHCR 优先、失败回退本地构建） | `deploy.sh` |
| H15 | 角色脚本化（镜像保持原始状态，按需切 persona） | `role-scripts/` + 管理端口「角色设定」卡片 |
| H16 | 容器内装 Xvfb 供无头自动化 | `xvfb` 已装 |
| H17 | fnOS/NAS（192.168.31.100）部署、端口映射、ghcr 拉取与「悬空标签」修复 | 运维经验沉淀 |
| **H18** | TFG 登录用 xRDP 可见浏览器（规避 webauth「无头陷阱」） | `TFG_LOGIN_MODE=xrdp`（默认） |

> 说明：**H9 / H10 / H18** 是你早前提出、曾被遗漏的要求，本轮已补回并强化。

---

## 3. 最终需求清单（模块化）

### 模块 A — 基础镜像与构建流水线
- **A1【已有】** 基于官方 `node:24-trixie` 构建，不写死第三方基础镜像。
- **A2【已有】** GitHub Actions 推送即构建并发布 GHCR（`:latest` / 版本 / `:sha-xxx`），用内置 `GITHUB_TOKEN`，无需额外 Secret。
- **A3【已有】** 镜像内不含任何 API Key / 令牌；敏感配置首次启动由 entrypoint 按环境变量生成。
- **A4【已有】** 源自适应：默认官方/全球源（适配 GHCR 境外 runner）；国内本地构建可用 `build-arg`（DEBIAN_MIRROR / NODE_DIST / NPM_REGISTRY）加速。
- **A5【核心·已落地】** TFG 二进制在**镜像构建阶段**用 Dockerfile `RUN` 命令从官方 `github.com/andeya/token-free-gateway` Releases **直接下载进镜像**，**不采用「宿主机本地下载后再 COPY 进镜像」**。
  - 仅允许官方源，禁用镜像站 / fork / 第三方转存；
  - 下载后用官方同版本 `checksums-sha256.txt` 做 sha256 校验，失败 `exit 1`；
  - 支持 `--build-arg TFG_VERSION=latest|vX.Y.Z`（默认 `latest`）；
  - 下载具备 `--retry 5` 重试。
- **A6【优化】** 构建日志对 TFG 下载/校验/安装各步骤打印明确标记。
- ~~A7（本地构建可跳过 TFG 下载开关）~~ **已按 Q2 移除**——恒为官方下载，不提供跳过开关。

### 模块 B — 模型与提供商
- **B1【已有】** 多模型一键切换 `MODEL_CHOICE` 1–5。
- **B2【已有】** 自定义 OpenAI 兼容（vLLM / 自建网关）。
- **B3【已有】** 模型名对齐：API 侧 `deepseek-v4-flash/pro`；TFG 侧各站「网页模型名」（如 `deepseek-chat`）。
- **B4【优化·保留建议】** 管理端口增「重新生成 settings.yaml」按钮，免删除容器换模型（尚未实现，列为后续）。

### 模块 C — 远程接入与桌面
- **C1【已有】** Xfce4 + xRDP(3389) + OpenSSH(22)。
- **C2【已有】** 构建期放开 `startup.ts` 对 `--host 0.0.0.0` 的拒绝。
- **C3【已有】** 角色脚本切换（`deepseekharness_role.sh` / `openclaw_role.sh`）。
- C4【原建议：SSH/xRDP 首启随机强密码】**按 Q3 不采纳**——保留固定 `deepseek`，运行容器用 `-e ROOT_PASSWORD=xxx` 可覆盖（覆盖动作记入审计）。

### 模块 D — 三大网关
- **D1【已有】** DeepSeek Harness Web (3080)。
- **D2【已有】** OpenClaw 网关 (18789) 八专家多角色。
- **D3【已有】** Hermes Web UI (3000) + 管理面板 (8080)。
- **D4【已有】** crypto.randomUUID polyfill（服务端 + 浏览器端）。
- **D5【优化·保留建议】** 为三大网关 + Web 提供统一健康探测，管理端口聚合展示（基础能力由 E4 提供）。

### 模块 E — 管理端口 16688
- **E1【已有】** 首启随机 `admin-xxxx` 凭据 + 强制改密；在线改 OpenClaw/Hermes 令牌/模型/Key + 热重启。
- **E2【已有】** 角色切换一键应用；「快速访问」卡片（带 token 直链）。
- **E3【优化·保留建议】** 在线编辑 TFG 配置（TFG_API_KEY / TFG_CDP_URL / TFG_PORT / 开关）并热重启。
- **E4【新增·已纳入 Q4·已实现】** 聚合健康/状态页：`/status`（免登录只读概览）+ `/api/status`（JSON），一览 8 项服务（Harness Web / OpenClaw / Hermes Web / Hermes 面板 / TFG / SSH / xRDP / Chromium CDP）的端口可达性。
- **E5【新增·已纳入 Q4·已实现】** 操作审计日志：登录、改密、改配置、TFG 应用、捕获、生成令牌、应用角色、重启等写入 `/var/log/dsh-audit.log`（谁/何时/做了什么）。

### 模块 F — Token-Free Gateway（核心）
- **F1【已有】** 内置官方 TFG，OpenAI 兼容 `/v1`，免 API Key 调 13+ 家模型。
- **F2【已有】** 构建期官方下载 + sha256 校验（见 A5）。
- **F3【核心·已落地】** 运行时 `-e ENABLE_TOKEN_FREE_GATEWAY=1` 启用；**默认开启**（0/false/空 关闭）；开关接受 `1 / true / yes / on / enabled`（大小写不敏感）。
- **F4【已有】** 两种登录模式：`xrdp` 可见浏览器（推荐）/ `headless` 无头 Chromium。
- **F5【优化·保留】** 默认挂载持久卷 `dsh-tfg-auth`(`/root/.token-free-gateway`) 与 `dsh-chrome-tfg`(`/root/.chrome-tfg-debug`)，重启免重新授权。
- **F6【新增·已纳入 Q4·已实现】** **自动转发根治 13456 不通**：TFG 实际监听内部端口（`TFG_INNER_PORT`，默认 34560），由 `socat` 自动转发 `0.0.0.0:3456 → 127.0.0.1:34560`，无论官方二进制默认绑 127.0.0.1 还是 0.0.0.0，宿主 13456 转发都可达。
- **F7【新增·保留】** `/health` 接入状态页；`/v1/models` 可用模型清单。
- **F8【新增·保留】** 安全护栏：检测到公网 IP 且未设 `TFG_API_KEY` 时告警（见 G3）。

### 模块 G — 安全
- **G1【已有】** 零密钥镜像；Key 仅运行时注入。
- **G2【优化】** 凭据策略：管理端口强制改密；SSH/xRDP 密码被 `-e` 覆盖时记入审计（随机强密码按 Q3 暂不实施）。
- **G3【新增·已纳入 Q4·已实现清单】** 公网部署检查清单（防火墙 / 反代+鉴权 / TLS / 默认密码告警），见技术方案.md 第 6 节。
- **G4【已有】** TFG 不鉴权时仅内网；设 `TFG_API_KEY` 后全接口鉴权。

### 模块 H — 部署与运维
- **H1【已有】** `deploy.sh` 一键部署（GHCR 优先，回退本地构建）。
- **H2【已有】** 数据卷持久化（`dsh-data`）。
- **H3【优化】** `deploy.sh` / README / Dockerfile 端口映射与默认值保持一致（本次已统一 TFG 默认开）。
- **H4【新增·保留】** 数据卷备份/恢复脚本；可选 watchtower 自动更新（尚未实现）。

### 模块 I — 文档
- **I1【待办】** 确认需求后，重新生成详细 `README.md`（涵盖全部模块、端口表、故障排查）。
- **I2【新增】** 独立的「Token-Free Gateway 快速上手」与「公网安全部署」小节/文档。

---

## 4. 端口与映射表（建议统一版）

| 容器内端口 | 建议宿主映射 | 服务 | 默认开关 |
|-----------|------------|------|---------|
| `3080` | `13000` | Harness Web UI（官方默认 3080） | 开 |
| `22` | `10022` | SSH（root / 自定义） | 开 |
| `3389` | `13389` | xRDP 远程桌面 | 开 |
| `18789` | `18789` | OpenClaw 网关（八专家多角色，0.0.0.0） | 开 |
| `3000` | `18000` | Hermes Web UI（需登录令牌） | 开 |
| `8080` | `18080` | Hermes 管理面板（需认证 provider） | 开 |
| `3456` | `13456` | Token-Free Gateway（OpenAI 兼容 /v1，对外经 socat 暴露） | **开（F3/Q1）** |
| `16688` | `16688` | 管理端口（在线配置，首启随机凭据） | 开 |

> 注：TFG 实际监听内部端口 34560，由 socat 转发到 3456（F6）。`13456:3456` 对外即 TFG 的 OpenAI 兼容端点。

---

## 5. 验收标准（如何判断达到预期）

1. **构建**：GitHub Actions 构建日志出现 `[TFG] 校验通过 (...)` 且 `token-free-gateway --version` 正常；镜像不含任何预下载的 TFG 二进制 COPY（来源唯一为官方 Releases）。
2. **TFG 默认开**：不加 `-e ENABLE_TOKEN_FREE_GATEWAY` 时，容器启动后 `13456` 端口对宿主可达，返回 OpenAI 兼容 `/v1`（如 `/v1/models` 或 `/health`）。
3. **开关归一化**：`-e ENABLE_TOKEN_FREE_GATEWAY=true|yes|on|enabled` 均可启用；`-e ...=0|false|off` 关闭。
4. **13456 根治**：无论官方二进制默认绑 127.0.0.1 还是 0.0.0.0，宿主 `13456` 均可达（socat 自动转发，F6）。
5. **状态页**：管理端口 `/status` 免登录展示 8 项服务存活；`/api/status` 返回 JSON（需登录）。
6. **审计**：登录、改密、改配置、TFG 应用、捕获、生成令牌、重启等操作均出现在 `/var/log/dsh-audit.log`，含时间戳/用户/动作/详情。
7. **密码策略**：未设置 `-e ROOT_PASSWORD` 时默认 `deepseek`；设置后被记入审计。
8. **公网清单**：README/文档提供 G3 公网部署检查清单。

---

## 6. 不在本次范围（明确排除项）

- ARM64 镜像（A8，按 Q5 暂不做）。
- 本地构建「跳过 TFG 下载」开关（A7，按 Q2 移除）。
- SSH/xRDP 首启随机强密码（C4/G2，按 Q3 不采纳）。
- TFG 版本锁定（按 Q6 保持 `latest`）。
- 数据卷备份脚本（H4）、管理端口在线编辑 TFG 配置（E3）、重新生成 settings.yaml 按钮（B4）、`/v1/models`（F7）、公网 API Key 强制（F8）等——列为后续增强，本次未实现。

---

*请审阅以上需求是否达到你的预期。确认后我将：① 复查代码（含本次 Dockerfile/entrypoint/mgmt.py 改动）；② 重新生成详细 `README.md`。*
